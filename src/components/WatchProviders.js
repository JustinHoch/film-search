function WatchProviders({ providers }) {
  if(providers){
    // TODO: Display watch providers for Buy, Rent, or Stream (if present)
    return (
      <div>
        
      </div>
    )
  }
}

export default WatchProviders
